# Python Baseline Note

This optional benchmark compares product aggregation with summing precomputed log returns in Python.
It is retained as background and is not part of the SQL Server storage experiment.

```bash
cd notes/python-baseline
python benchmark.py
python -m unittest
```
